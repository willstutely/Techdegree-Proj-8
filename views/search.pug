form.search(action=`/books/search/testing?search=${searchInput}`, method="post")
    div
        //- input(type="search", name="id", placeholder="Search by ID...")
        //- input(type="search", name="title", placeholder="Search by Title...")
        //- input(type="search", name="author", placeholder="Search by Author...")
        //- input(type="search", name="genre", placeholder="Search by Genre...")
        //- input(type="search", name="year", placeholder="Search by Year...")
        
        input(type="text", name="search", placeholder="Search Library...")

        
        input.search(type="submit" value="Search")

